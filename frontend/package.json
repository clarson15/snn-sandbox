{
  "name": "snn-sandbox-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "benchmark": "node --experimental-specifier-resolution=node ./scripts/run-deterministic-benchmarks.mjs",
    "benchmark:compare": "node --experimental-specifier-resolution=node ./scripts/run-benchmark-comparison.mjs",
    "benchmark:render-culling": "node --experimental-specifier-resolution=node ./scripts/benchmark-render-culling.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.1.0",
    "@vitejs/plugin-react": "^4.3.2",
    "jsdom": "^25.0.1",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
