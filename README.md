# snn-sandbox

## TODO
_Remove when completed_
- Set up frontend/ React app
- Configure Dockerfile to run frontend tests and include frontend build
- Update README.md with description of project, features, installation, usage
- Set up semver tagging and store tag in Dockerfile environment as environment variable `APP_VERSION`